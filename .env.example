# 复制这个文件，重命名为 .env，然后把下面这行改成你自己的 MongoDB 连接串
# 连接串在 MongoDB Atlas 后台的「Connect → Drivers」里能找到
# 注意：.env 文件不会提交到 git（已在 .gitignore 里排除），密码安全

MONGODB_URI=mongodb+srv://用户名:密码@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
